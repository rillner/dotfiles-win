# ~/.config/starship.toml

add_newline = false
command_timeout = 1500
format = """$os$shell$username$hostname$directory$kubernetes$helm$terraform$git_branch$git_status"""

# Drop ugly default prompt characters
[character]
success_symbol = ''
error_symbol = ''

# ---

[os]
format = '[$symbol](bold white) '   
disabled = false

[os.symbols]
Windows = ''
Arch = '󰣇'
Ubuntu = ''
Macos = '󰀵'

[username]
style_user = 'white bold'
style_root = 'black bold'
format = '[$user]($style) '
disabled = false
show_always = true

[hostname]
ssh_only = false
format = 'on [$hostname](bold yellow) '
disabled = false

[directory]
truncation_length = 1
truncation_symbol = '…/'
home_symbol = '󰋜 ~'
read_only_style = '197'
read_only = '  '
format = 'at [$path]($style)[$read_only]($read_only_style) '

[kubernetes]
symbol = '󱃾 '
format = 'via [$symbol$context\($namespace\)](bold purple) '
disabled = false

[helm]
symbol = ' '
format = 'via [$symbol$version](bold purple) '
disabled = false

[terraform]
symbol = '󱁢'
version_format = ' v${raw}'
format = 'via [$symbol$version $workspace](bold purple) '
disabled = false

[git_branch]
symbol = ' '
format = 'via [$symbol$branch]($style) '
# truncation_length = 4
truncation_symbol = '…/'
style = 'bold green'

[git_status]
use_git_executable = true
format = '[$all_status$ahead_behind]($style) '
style = 'bold green'
# Variables belonging to all_status
conflicted = '󰦦:${count} '
stashed = '📦:${count} '
deleted = ':${count} '
renamed = '󰬵:${count} '
modified = ':${count} '
typechanged = 'Changed Type:${count} '
staged = '/:${count} '
untracked = ':${count} '
# Variables belonging to ahead_behind
diverged = '⇕⇡${ahead_count} ⇣${behind_count}'
ahead = '⇡${count}'
behind = '⇣${count}'
up_to_date = ''
# Path to the Starship executable on Windows for WSL usage
windows_starship = '/mnt/c/Program Files/starship/bin/starship.exe'

# ---

[shell]
format = '[\[$indicator\]]($style) '
disabled = true

[vagrant]
disabled = true

[docker_context]
disabled = true

[python]
disabled = true

[nodejs]
disabled = true

[ruby]
disabled = true